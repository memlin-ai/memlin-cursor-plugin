Summarize the installed `/memlin-*` commands by purpose: context and search,
status/setup, project linking, sync/plans, actions, audit, and coordination.
When `memlin` is available, run `memlin help` and relay its host-aware command
reference verbatim. A Marketplace install alone does not provision that
launcher; if it is absent, explain that CLI-only commands require the signed
macOS/Linux bootstrap after removing any Marketplace copy, or the direct
bundled CLI from a Companion-managed local install on Windows.
